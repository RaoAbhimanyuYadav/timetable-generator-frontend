import { Button, Typography } from "@mui/material";
import React from "react";
import TableLayout from "../common/TableLayout";

const Timing = () => {
  return (
    <>
      <Typography>Timing</Typography>
      <Typography>Add More Time</Typography>
      <TableLayout />
    </>
  );
};

export default Timing;
